export interface Meta {
  bikoId?: number;
  title: string;
  subtitle: string;
  year: number;
  stage: string;
  codebattle?: boolean;
}

export interface PostTheme {
  background: string;
  color: string;
}
